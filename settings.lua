config = {
    storage = {},
    graphics = {},
    money = 10000,
    rounds = 5,
    colors = {
        green = Utils.colorFromHex("5cba0a"),
        blue = Utils.colorFromHex("b1d8ff"),
    }
}

Debug = {
    showFps = 1,
    mousePos = 1,
    drawUiDebug = false,
    PrintAnimationEvents = false,
}
