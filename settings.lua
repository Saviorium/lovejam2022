config = {
    storage = {},
    graphics = {},
    money = 3000,
    moneyPerRound = 1000,
    rounds = 5,
    roundTime = 15,
    colors = {
        green = Utils.colorFromHex("5cba0a"),
        blue = Utils.colorFromHex("b1d8ff"),
    }
}

Debug = {
    showFps = 0,
    mousePos = 0,
    drawUiDebug = false,
    PrintAnimationEvents = false,
}
